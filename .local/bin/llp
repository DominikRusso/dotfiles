#!/bin/sh

# list largest installed packages
# control number of packages to list with first argument

if [ -n "$1" ]; then
    num=$1
else
    num=20
fi

expac '%m %n' | sort -h | tail -n $num | numfmt --to=iec

